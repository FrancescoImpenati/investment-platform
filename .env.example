# Phase 0 does not read this file and requires no provider credentials.
# Copy it to .env only after a provider adapter explicitly introduces a runtime consumer.
# Never commit the populated .env file.

MASSIVE_API_KEY=
SECONDARY_PROVIDER_API_KEY=
